// Public IDs (rendered into the HTML). Fill once the accounts are created.
export const UMAMI_WEBSITE_ID = ''; // Umami Cloud → Settings → Websites
export const WEB3FORMS_ACCESS_KEY = ''; // web3forms.com (tied to the receiving email)
