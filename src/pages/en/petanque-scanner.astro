---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PetanqueLanding from '../../components/PetanqueLanding.astro';
import { copy, OG_IMAGES, LANDING_PATHS } from '../../data/petanqueScanner';

const t = copy.en;
---

<BaseLayout title={t.title} description={t.description} image={OG_IMAGES.en} lang="en" alternates={LANDING_PATHS}>
	<PetanqueLanding lang="en" />
</BaseLayout>
