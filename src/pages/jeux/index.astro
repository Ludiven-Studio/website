---
import BaseLayout from '../../layouts/BaseLayout.astro';
import GameCard from '../../components/GameCard.astro';
import SuggestionForm from '../../components/SuggestionForm.astro';
import { games } from '../../data/games';

const NUM_FR = ['zéro', 'un', 'deux', 'trois', 'quatre', 'cinq', 'six', 'sept', 'huit', 'neuf', 'dix', 'onze', 'douze', 'treize', 'quatorze', 'quinze', 'seize'];
const playable = games.filter((g) => g.status === 'play').length;
const playableLabel = NUM_FR[playable]
	? NUM_FR[playable].charAt(0).toUpperCase() + NUM_FR[playable].slice(1)
	: String(playable);

const logique = games.filter((g) => (g.category ?? 'logique') === 'logique');
const adresse = games.filter((g) => g.category === 'adresse');
const multi = games.filter((g) => g.category === 'multi');
---

<BaseLayout
	title="Mini-jeux | Ludiven Studio"
	description="Une collection de mini-jeux de logique et d'adresse à entraîner chaque jour, signés Ludiven Studio."
>
	<div class="wrapper stack gap-8 lg:gap-20">
		<header class="games-hero stack gap-2 lg:gap-4">
			<h1>Mini-jeux</h1>
			<p>
				Des mini-jeux pensés pour aiguiser ta logique ou tes réflexes en quelques minutes.
				{playableLabel} à découvrir — et d'autres arrivent.
			</p>
			<ul class="games-promise" aria-label="Nos engagements">
				<li>🆓 Gratuit</li>
				<li>🚫 Sans publicité</li>
				<li>🔓 Sans inscription</li>
			</ul>
			<a class="games-defi-cta" href="/jeux/defi">🏆 Le défi du jour</a>
		</header>

		<section class="games-section stack gap-6">
			<h2 class="games-section-title">Jeux de logique</h2>
			<ul class="games-grid">
				{
					logique.map((game) => (
						<li>
							<GameCard
								id={game.id}
								title={game.title}
								pitch={game.pitch}
								icon={game.icon}
								href={game.href}
								status={game.status}
								image={game.image}
								favorite
							/>
						</li>
					))
				}
			</ul>
		</section>

		{
			adresse.length > 0 && (
				<section class="games-section stack gap-6">
					<h2 class="games-section-title">Jeux d'adresse</h2>
					<ul class="games-grid">
						{adresse.map((game) => (
							<li>
								<GameCard
									id={game.id}
									title={game.title}
									pitch={game.pitch}
									icon={game.icon}
									href={game.href}
									status={game.status}
									image={game.image}
									favorite
								/>
							</li>
						))}
					</ul>
				</section>
			)
		}

		{
			multi.length > 0 && (
				<section class="games-section stack gap-6">
					<h2 class="games-section-title">Jeux multis</h2>
					<ul class="games-grid">
						{multi.map((game) => (
							<li>
								<GameCard
									id={game.id}
									title={game.title}
									pitch={game.pitch}
									icon={game.icon}
									href={game.href}
									status={game.status}
									image={game.image}
									daily={false}
									favorite
								/>
							</li>
						))}
					</ul>
				</section>
			)
		}

		<SuggestionForm />

		<p class="games-note">
			Des mini-jeux de logique développés par Ludiven Studio, librement inspirés de grands
			classiques du genre. Sans affiliation avec leurs éditeurs respectifs.
		</p>
	</div>
</BaseLayout>

<style>
	.games-note {
		text-align: center;
		max-width: 60ch;
		margin-inline: auto;
		color: var(--gray-400);
		font-size: var(--text-sm);
	}

	.games-hero {
		text-align: center;
		max-width: 50ch;
		margin-inline: auto;
		color: var(--gray-300);
		font-size: var(--text-md);
	}

	.games-hero h1 {
		font-size: var(--text-3xl);
	}

	.games-promise {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: 8px;
		justify-content: center;
		padding: 0;
		margin: 0.25rem 0 0;
	}
	.games-promise li {
		background: var(--gray-999);
		border: 1px solid var(--gray-800);
		border-radius: 999px;
		padding: 5px 13px;
		font-size: var(--text-sm);
		font-weight: 600;
		color: var(--gray-100);
	}
	.games-defi-cta {
		align-self: center;
		margin-top: 0.5rem;
		background: var(--accent-regular);
		color: var(--accent-text-over);
		font-family: var(--font-brand);
		font-weight: 700;
		font-size: var(--text-md);
		text-decoration: none;
		border-radius: 999px;
		padding: 10px 22px;
		box-shadow: var(--shadow-md);
		transition: filter var(--theme-transition);
	}
	.games-defi-cta:hover,
	.games-defi-cta:focus-visible {
		filter: brightness(1.06);
	}

	.games-section-title {
		font-size: var(--text-xl);
		text-align: center;
		margin: 0;
		color: var(--gray-0);
	}

	.games-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 1.5rem;
		list-style: none;
		padding: 0;
		margin: 0;
	}

	@media (min-width: 50em) {
		.games-hero {
			font-size: var(--text-lg);
		}

		.games-hero h1 {
			font-size: var(--text-4xl);
		}

		.games-grid {
			grid-template-columns: repeat(3, 1fr);
			gap: 2rem;
		}
	}
</style>
